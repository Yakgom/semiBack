<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="libraryMapper">
 
 <select id="login" resultType="libraryMember" parameterType="LibraryMember">
 	SELECT
 		   USER_NO userNo
 		 , USER_ID	userId
 		 , USER_PWD userPwd
 		 , USER_NAME userName
 		 , phone 
 		 , EMAIL
 		 , CHECKOUT
 		 , ENROLL_DATE enrollDate
 	 FROM
 	 	   LIBRARY_MEMBER
 	WHERE
 		   USER_ID = #{userId}
 	  AND
 	  	   USER_PWD = #{userPwd}
 	  AND
 	  	   STATUS='Y'
 </select>


<update id="update"	parameterType="LibraryMember">
	UPDATE
		   LIBRARY_MEMBER
	   SET	
	       PHONE = #{phone}
	     , EMAIL = #{email}
	 WHERE
	       USER_ID = #{userId}
	   AND
	       USER_PWD = #{userPwd}
	   AND
	       USER_NAME = #{userName}                       	   
</update>
 
 
 </mapper>